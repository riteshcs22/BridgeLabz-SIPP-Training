INTERFACE Searchable
    METHOD search(criteria)
END INTERFACE

INTERFACE Printable
    METHOD print()
END INTERFACE

CLASS ReportTypeA IMPLEMENTS Searchable, Printable
    METHOD search(criteria):
        // Search in ReportTypeA data
    METHOD print():
        // Print ReportTypeA format
END CLASS

CLASS ReportTypeB IMPLEMENTS Searchable, Printable
    METHOD search(criteria):
        // Search in ReportTypeB data
    METHOD print():
        // Print ReportTypeB format
END CLASS

CLASS MedicalRecordManager
    METHOD displayReport(Searchable s, Printable p):
        s.search(criteria)
        p.print()
END CLASS

MAIN:
    create ReportTypeA r1
    create ReportTypeB r2
    MedicalRecordManager m
    m.displayReport(r1, r1) // Polymorphism
    m.displayReport(r2, r2)
END
